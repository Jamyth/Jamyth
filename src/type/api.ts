export interface ProfileView {
    public_repos: number;
}

export interface RepositoryView {
    full_name: string;
    name: string;
    description: string;
    url: string;
    language: string;
}
